name: pixdrop
publish_to: 'none'
description: PixDrop inventory application
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  image_picker: ^1.1.2
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
